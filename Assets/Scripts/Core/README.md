# MOVED — delete this folder

All code that lived here has been moved and integrated into
`Assets/_Project/Scripts/Core/` (CBuilding.* namespaces).

Delete `Assets/Scripts` in the Unity Project window (right-click → Delete).
Every .cs file below has been emptied, so removing this folder is safe.
